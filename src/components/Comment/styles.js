import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    commentContainer: {
        flexDirection: 'row',
        alignItems: 'center',
        marginBottom: '2%'
    },
});

export default styles;